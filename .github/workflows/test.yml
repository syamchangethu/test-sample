name: Do Something That Needs Scheduled
on:
  push:
    branches:
      - main
jobs:
  build:
    name: Run Some Thing
    runs-on: ubuntu-latest
    steps:
      - name: Do Something
        run: echo Doing Something...
